package DSAJAVA.Array.Sorting;

public class QuickSort<T extends Number> {

    public void quickSort(T[] array,int start,int end){
//        int pivot = end;
        int index = start-1;
        for (int j = start;j<end;j++){
            if (array[j].doubleValue()<array[end].doubleValue())
                index++;
            else{

            }
        }
    }
}
